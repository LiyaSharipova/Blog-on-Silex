<!DOCTYPE html>
<html>
<head>
    <title>{% block title '' %} - My Silex Application</title>

    <link href="{{ asset('css/main.css') }}" rel="stylesheet" type="text/css"/>

    <script type="text/javascript">
        var _gaq = _gaq || [];
        _gaq.push(['_setAccount', 'UA-XXXXXXXX-X']);
        _gaq.push(['_trackPageview']);

        (function () {
            var ga = document.createElement('script');
            ga.type = 'text/javascript';
            ga.async = true;
            ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
            var s = document.getElementsByTagName('script')[0];
            s.parentNode.insertBefore(ga, s);
        })();
    </script>
    {#/home/liya/programming/Silex-Project/vendor/tinymce/tinymce/tinymce.min.js#}
    <script src="{{ asset('js/tinymce/tinymce/tinymce.min.js') }}"></script>
    {#<script src="https://cloud.tinymce.com/stable/tinymce.min.js"></script>#}
    <script type="text/javascript">
        tinymce.init({
            selector: '#mytextarea'
        });
    </script>

</head>
<body>
<ul>
    {% if app.user %}
        <li><a href="{{ app.url_generator.generate('profile') }}">Profile</a></li>
        <li><a href="{{ app.url_generator.generate('addPost') }}">Make Post</a></li>
        <li><a href="{{ path('user_logout') }}">Log out</a></li>

    {% else %}

        <li><a href="{{ app.url_generator.generate('login') }}">Log in</a></li>
        <li><a href="{{ app.url_generator.generate('signup') }}">Sign Up</a></li>

    {% endif %}

</ul>
{% block content %}{% endblock %}
</body>
</html>
